// Just noticeable difference if ciede2000 >= JND then colors difference is noticeable by human eye
export const JND = 2.3;
export const REQUIRED_IMAGE_FIELDS = ["source", "boundingBox"];
export const REQUIRED_BOUNDING_BOX_FIELDS = ["left", "top", "right", "bottom"];
export const CLUSTERS_SIZE = 10;
